import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Корзина | Магазин мерча Warpoint"
}

type CartPageProps = {};

export default function CartPage({}: CartPageProps) {
  return <div></div>;
}
